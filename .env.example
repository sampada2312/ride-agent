# Optional: set this to enable the OpenAI-backed agent mode.
# The demo works without an API key and defaults to the local deterministic mode.
OPENAI_API_KEY=

# Optional: override the model used when OPENAI_API_KEY is set.
OPENAI_MODEL=gpt-5
